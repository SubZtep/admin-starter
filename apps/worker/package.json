{
  "name": "@app/worker",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "zdev": "exit 0; bun run --hot cli.ts",
    "dev": "bun run --hot cli.ts",
    "testerhelper": "bun run --hot testerhelper.ts",
    "build": "bun build ./cli.ts --compile --minify --sourcemap --bytecode --outfile mycli"
  },
  "dependencies": {
    "@app/schemas": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.11"
  }
}
