# Baked into the compiled binary for `build:release` / `bun run build:cli` (Bun --env=API_*).
# Resolution order: --api-url > API_URL > env-paths config.json(apiUrl) > default.
API_URL=http://localhost:3001
