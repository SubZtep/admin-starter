{
  "name": "@app/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir dist",
    "start": "bun x auth@latest migrate && bun run dist/index.js"
  },
  "dependencies": {
    "@app/schemas": "workspace:*",
    "@app/shared": "workspace:*",
    "better-auth": "^1.5.2",
    "hono": "^4.5.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.19.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/pg": "^8.18.0"
  }
}
