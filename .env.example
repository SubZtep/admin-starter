# Compose-level vars (auto-loaded by `docker compose`).
# Used as build args for the web image — these get baked into the client bundle.
VITE_API_URL=http://localhost:3001
VITE_APP_URL=http://localhost:3000
